# CLAUDE.md

## 作業開始時のチェックリスト

1. `docs/product.md` を読みプロダクトを理解する（機能一覧は `README.md`）
2. `docs/architecture.md` で実装方針を確認する
3. GitHub Issues / Milestone でタスクの状態・実装順を確認する

---

## ドキュメント構成ルール

実装・コード生成・DB設計を行う前に、以下のドキュメントが `docs/` 配下に揃っていることを確認すること。
不足しているドキュメントがある場合は、作業開始前にユーザーに確認または作成を促す。

### 必須ドキュメント

各ドキュメントは以下の必須セクションを含むこと。セクションが欠けている場合は作業前に追記する。

| ファイル | 役割 | 必須セクション |
|----------|------|--------------|
| `docs/product.md` | プロダクトの目的・対象ユーザー・ゴール | 目的、対象ユーザー、成功指標 |
| `docs/architecture.md` | 技術スタック・実装方針 | 技術スタック、実装方針、環境変数 |
| `docs/ui.md` | 画面一覧・遷移・UI規約 | 画面一覧、画面遷移図（Mermaid）、画面機能仕様、各画面の表示状態（Loading / Empty / Error）、UI規約 |
| `docs/development.md` | 開発・運用手順 | ローカルセットアップ、環境変数、DB操作（該当する場合）、デプロイ手順 |

> 進捗・タスク管理は GitHub Issues / Milestone で行う（ドキュメントには持たない）。自動テスト方針は本ファイルの「テストルール」を参照。UI の動作確認は Issue ごとの動作確認チェックリストで行う（[UI実装の動作確認ルール](#ui実装の動作確認ルール)）。

### 条件付き必須ドキュメント

以下は該当する実装・運用が存在する場合にのみ作成を必須とする。

| 条件 | ファイル | 役割 | 必須セクション |
|------|---------|------|--------------|
| 外部 REST API を提供する場合 | `docs/api.md` | 外部 REST API の概要・認証・一覧（フィールド単位の契約は生成した OpenAPI 仕様が正） | エンドポイント一覧、認証、エラーレスポンス定義、公開リファレンス（Zod スキーマから生成した OpenAPI/Scalar）へのポインタ。**フィールド単位のリクエスト・レスポンス定義は手書きせず生成物（`openapi.json`）を正とする**（重複・drift を避けるため） |
| Server Actions を採用する場合 | `docs/actions.md` | Server Actions の一覧・共通仕様（引数/戻り値/エラーの詳細はコード・テストが正） | Action 一覧、共通仕様 |
| 永続化 DB を利用する場合 | `docs/schema.md` | リレーション・各カラムの意味（スキーマ本文は `prisma/schema.prisma` が正） | リレーション図、カラムの意味説明、インデックス |
| 認証・認可フローがある場合 | `docs/auth.md` | 認証フロー・保護対象ルート・セッション管理 | 認証フロー、保護対象ルート、セッション管理 |
| 外部サービス連携がある場合 | `docs/integrations.md` | 外部サービス連携フロー・接続設定 | サービス一覧、連携フロー、環境変数 |
| デプロイ・インフラ構成が必要な場合 | `docs/infra.md` | インフラ構成・デプロイ手順 | 構成図、環境一覧、デプロイ手順 |

### README.md

`README.md` はリポジトリルートに配置し、GitHub のトップページに表示されるランディングページとして機能させる。

**記載する内容（他のドキュメントと重複させない）：**

| セクション | 内容 |
|-----------|------|
| プロダクト名・概要 | 一文でプロダクトの目的を説明 |
| 機能 | 主要機能の箇条書き（詳細は `docs/product.md` に委譲） |
| ドキュメント | `docs/` 配下の全ドキュメントへのリンクテーブル |
| クイックスタート | 起動に最低限必要なコマンドのみ。詳細は `docs/development.md` へのリンクで委譲 |

---

## ドキュメント更新ルール

### 何をドキュメント化するか（source of truth）

- **コードが正の内容はドキュメント化しない**。コンポーネント一覧・ディレクトリ構成・具体的な CSS/Tailwind クラス・Prisma スキーマ本文・Server Action のシグネチャなどは、乖離時に**コードを正**とし、ドキュメントには書かない（書くと更新コストと乖離だけが増える）
- ドキュメントに残すのは **意図・契約・規約**：プロダクトの目的、意思決定の理由、外部 API 契約（`api.md`）、UI 規約、運用手順、DB カラムの意味的説明など

### 更新のルール

- 実装中に仕様変更が発生した場合、上記「ドキュメントに残す」領域に該当するものだけ、コード修正と同時に更新する
- その領域でドキュメントとコードの乖離が生じた場合は、原則ドキュメントを正としてコードを修正する（意図的な仕様変更時はドキュメントを先に更新してからコードを変更）

---

## テストルール

### テスト方針

- 投資の優先度は **バックエンドの整合性（`lib/` ・ Server Actions ・ API ルート）> 型・lint >>> UI E2E**。UI E2E / コンポーネントテストは高コスト・低 ROI のため**書かない**
- UI は薄く保ち、非自明なロジックは `lib/` やカスタムフックに切り出してユニットテストする（「フロントは薄く、ロジックは lib へ」）
- 外部依存（Prisma・Clerk 等）は `vi.mock` でモックする。テストファイルは実装と同じディレクトリに `[name].test.ts` で配置し、先頭に `// @vitest-environment node` を付ける
- 実行: `npm test`（1回）/ `npm run test:watch` / `npm run test:coverage`

### 完了条件

- **API ルートの実装はユニットテストの作成をもって完了とする**
- **`lib/` 配下のユーティリティ関数・Server Actions もユニットテストの作成をもって完了とする**
- UI コンポーネントのユニットテスト・E2E は必須としない（動作確認は Issue の動作確認チェックリストで行う）

### テストの書き方

- ファイル先頭に `// @vitest-environment node` を付ける
- `vi.mock("@/lib/prisma", ...)` で Prisma をモックする。Prisma を直接参照しない場合は必要な `@/lib/*` をモックする
- `beforeEach` で `vi.clearAllMocks()` を呼ぶ。モック関数は `vi.mocked()` で型付き参照する
- トップレベルの `describe()` は関数名、`it()` は日本語でケースを説明する。**正常系と異常系を分けて**記述する

### 最低限カバーすべき観点

- **API ルート（`src/app/api/**/route.ts`）**: 正常系 / バリデーションエラー(400) / 認証エラー(401) / 認可エラー(403・該当時) / 未存在(404・該当時) / 重複(409・該当時)
- **ユーティリティ関数（`src/lib/`）**: 正常系 / 境界値・エッジケース（空文字・フォーマット違反・範囲外など）
- **Server Actions**: 正常系（戻り値）/ 未認証 redirect / lib の `{ error }` 返却 / 予期しない例外時の `{ error }`

---

## UI実装の動作確認ルール

- **UI を含む実装はアプリを起動した手動動作確認をもって完了とする**
- 実装完了後、コミット・プッシュの前に動作確認一覧を **Issue にコメントとして投稿** し、ユーザーに提示する
- ユーザーが動作確認を実施し、問題がなければコミット・プッシュに進む
- 動作確認中に一覧の追加・変更が発生した場合は、**新しいコメントを追加せず、最初に投稿したコメントを編集して更新する**

### 動作確認チェックリストの形式

以下の観点を網羅した表形式で投稿する：

| # | ユーザー | 手順 | 確認観点 | 期待値 |
|---|---|---|---|---|
| ... | ... | ... | ... | ... |

> 認証・ロール分離がないアプリでは「ユーザー」列は省略してよい。

### 最低限カバーすべき観点

- **認証・認可**: 未ログイン時のリダイレクト、ログイン成功・失敗
- **画面表示**: データが正しく表示されるか、空状態の表示
- **インタラクション**: ボタン・フォームの操作、状態変化（選択・入力）
- **保存・更新**: 保存後のフィードバック表示、リロード後のデータ永続化
- **ユーザー分離**: 複数ユーザーでデータが混在しないか（該当する場合）

---

## バージョン準拠ルール

- フレームワーク・ライブラリのバージョンは **常に `package.json` を確認して**、そのバージョンの仕様に従う
- 古いバージョンの知識や一般的な慣習を根拠にしない
- 外部ツール（Copilot 等）やレビュアーからフレームワーク・ライブラリの慣習に関する指摘を受けた場合、`package.json` のバージョンに基づく仕様を確認してから採否を判断する
- バージョン固有の仕様・既知の指摘パターンは `docs/architecture.md` に記載する

---

## コーディングルール

- コメント: 自明でないロジックにのみ付ける（過剰なコメント不要）
- エラーハンドリング: 外部入力・API レスポンスの境界でのみ行う
- 内部コードやフレームワークが保証している箇所に防御的コード（不要な null チェック・型ガード）を追加しない
- 言語・ツール・フォーマッタなどプロジェクト固有の選択は `docs/architecture.md` を参照

---

## セキュリティルール

- **ユーザーデータ分離**: DB アクセスは `where` に `userId` を含めて絞り込む、または取得後に所有者の `userId` を検証する。全 Server Action・API ルートでログイン済みユーザーの確認（`getSession()` / API キー認証）を行う
- **リクエストパラメータのユーザー ID を信頼しない**: 操作対象ユーザーは常にセッション（`getSession()`）から取得する
- **SQL**: `$queryRaw` / `$executeRaw` を文字列連結・未サニタイズ入力で組み立てない。未検証の入力を `orderBy` / 動的キー / フィールド名にそのまま使わない
- **XSS**: `dangerouslySetInnerHTML` は使用しない

---

## GitHub Issues 管理ルール

新規タスク・不具合はすべて GitHub Issues で管理する。

### ブランチ・マージルール

- `develop` および `master` へのマージは **必ず PR を作成**し、ユーザーの承認を得てからマージする
- 直接マージ（`git merge`）は行わない

### Issue 作成ルール

| 項目 | ルール |
|------|--------|
| タイトル | `[T番号] タスク名` の形式（例: `[T60] XXX機能追加`） |
| T番号 | 直前の最大番号 + 1 で採番する |
| Milestone | 対象フェーズの Milestone をアサインする |
| Label | `enhancement`（新機能）/ `fix`（不具合）/ `testing`（テスト）/ `documentation`（ドキュメント）/ `ci`（CI/CD・GitHub Actions・Dependabot などインフラ寄りの変更）から選ぶ |
| 本文 | 概要・要件・依存 Issue を記載する |

### Issue クローズルール

Issue をクローズする際は、**必ず関連するコミット ID をコメントに含める**。

```bash
gh issue close <番号> --comment "完了。実装コミット: <short-hash> (<コミットメッセージ>)

<補足説明（任意）>"
```

---

## バージョニングルール

- `master` への PR に `bump:minor`（新機能）または `bump:patch`（バグ修正・小改善）ラベルを**1つだけ**付ける。`bump-version.yml` が `package.json` を自動バンプするため手動更新は不要
- SemVer に従う。メジャーバンプ（`1.0.0` 等）はユーザーの明示的な指示時のみ
- リリースフロー・CI・Dependabot 対応の詳細は [`docs/operations.md`](docs/operations.md) を参照

---

## Git 操作ルール

- `git commit` / `git push` はユーザーの **明示的な承認（「OK」など）を受けてから** 実行する
- 実装・ドキュメント更新が完了したら、変更内容をユーザーに提示して承認を待つ
- 承認前にコミット・プッシュを自動で行わない

### Issue 着手フロー

Issue に着手する際は、以下の順で進める：

1. Issue の仕様・現状コードを確認し、作業タスクを整理する
2. **Issue にコメントとして**作業タスク（チェックリスト形式）を投稿する
3. ユーザーに確認・承認を得る
4. `feature/t<番号>-<概要>` の形式でブランチを切る
5. 承認後に実装を開始する

> Issue 本文は「何をするか（仕様）」、コメントは「どうやるか（作業計画・進捗）」として使い分ける。

### Issue 単位の作業フロー

1 つの Issue に対応する実装が完了したら、以下の順で処理する：

1. 変更内容をユーザーに提示して承認を待つ
2. 承認後、`git commit` → `git push` を実行する
3. PR を作成し、ユーザーのレビューを待つ（マージは自分でしない）
4. マージ後、対応する GitHub Issue を関連コミット ID を添えてクローズする

### 作業スコープルール

- **指示されたタスク（Issue）のみを実行する**。ユーザーが明示的に指定していないタスクは、自己判断で着手しない
- 複数 Issue をまとめて依頼された場合も、1 Issue ごとに完了・承認を経てから次に進む
- 「マイルストーン X を進める」など範囲指定の指示を受けた場合は、着手前に対象 Issue の一覧と実施順をユーザーに提示して確認をとる

---

## PR レビュー指摘の対応ルール

- Copilot・レビュアーからの指摘は、マージ前に同一ブランチ内で対応する
- 対応不要と判断した場合はその理由をコメントで返す
- 指摘対応のコミットメッセージは `fix: <内容>` とする
- 指摘に対応したら、該当コメントに対応済みの旨をコミット ID を添えて返信し、スレッドを Resolve する

```
対応済み。修正コミット: <short-hash> (<コミットメッセージ>)

<補足説明（任意）>
```
